GET ALL FIELDS - we don't need the complicated schema query, we can just use fields(ownFields) ✓

Does content export include the root item in the results? Make sure it does! ✓

IMPORT FAILS ON NULL FIELDS - make it skip anything where value is N/A ✓ 

Image Import doesn't work - figure this out ✓ 

Clear out browse selections on Clear All  ✓ 

On clear all, reset save settings dropdown ✓

Import - CREATE message (change "updated" to "created") ✓

Make button ribbon sticky ✓

Import - add additional code to make importing easier
 > Convert raw media paths/GUIDs to image tags?
 > Convert path to ID? 
 > Export linked item names/IDs?